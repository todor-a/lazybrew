{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "lazybrew settings",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "theme": {
      "type": "string",
      "enum": ["Lazygit", "Bright", "Ocean", "Dracula"],
      "description": "Color theme. Cycle in-app with n."
    },
    "outdatedThreshold": {
      "type": "string",
      "enum": ["any", "minor", "major"],
      "description": "How big a version jump must be before a package is marked outdated. Unreadable versions are always marked."
    }
  }
}
